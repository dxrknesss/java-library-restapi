spring:
  datasource:
    url: jdbc:h2:mem:some_strange_name_for_a_database;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password: assw0rd
  jpa:
    hibernate:
      ddl-auto: create

hibernate:
  show-sql: true
